{
  "name": "gamifiai-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && esbuild index.tsx --bundle --outfile=dist/index.js --loader:.tsx=tsx --external:react --external:react-dom/client && sed -i 's|/index.tsx|/index.js|' dist/index.html"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}